Task to be completed 

1. Insert a new record in your Orders table.

2. 
2.1 Add Primary key constraint for SalesmanId column in Salesman table. 
2.2 Add default constraint for City column in Salesman table.
2.3 Add Foreign key constraint for SalesmanId column in Customer table. 
2.4 Add not null constraint in Customer_name column for the Customer table.

3. Fetch the data where the Customer’s name is ending with ‘N’ also get the purchase amount value greater than 500.

4. Using SET operators,
4.1 Retrieve the first result with unique SalesmanId values from two tables, 
4.2 and the other result containing SalesmanId with duplicates from two tables.

5. Display the below columns which has the matching data.
    Order: date, Salesman Name, Customer Name, Commission, and City which has the range of Purchase Amount between 500 to 1500.

6. Using right join fetch all the results from Salesman and Orders table.
